// hippo uses home grown service directory. flask app. source of truth for infrastructure.
// just gives data. then use cli generation tool to generate code.
// this is similar to software templates but instead of templates is strictly data which is returned
// by an API
