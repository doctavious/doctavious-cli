use std::fs::File;
use std::io::{self, Read, Write};
use tempfile::{tempdir, tempfile, NamedTempFile};

// init default
#[test]
fn init() {}

// init options

// init override existing

// new w/o init

// new with init
